class DoMaths {
    static add(x, y) {
        return x + y;
    }
    static subtract(x, y) {
        return x - y;
    }
    static square(x) {
        return x * x;
    }
}

let x = DoMaths.add(2,5);
console.log(x);